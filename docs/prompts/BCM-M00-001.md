# BCM-M00-001

Read `AGENTS.md` and `TASKS.md`. Safely synchronize the current local Beach Cocktails Merge project with `origin/main` while preserving owner work. Commit and push the reconciled baseline, write an evidence log under `docs/codex-logs/`, verify local and remote HEADs match, and stop for independent audit. Do not start visual integration in this work item.